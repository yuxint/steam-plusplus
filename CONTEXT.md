# CONTEXT.md — WattToolkit 改造版术语表

本文件只收术语定义，不收实现细节。实现决策见 `docs/adr/`。

## 术语

**改造版**
基于 WattToolkit（BeyondDimension/SteamTools 的 clone）锁死 fork 后裁剪出的 macOS 专用轻量应用，定位是替代官方 Steam++。只保留网络加速一个功能域。

**官方身份**
bundle id `net.steampp.app` 与数据目录 `~/Library/Steam++`。改造版沿用官方身份，卸载官方版后零配置继承既有数据（加速勾选、已信任根证书、加速分组缓存、启动配置）。改造版与官方版不可并存。

**加速分组（Project Group）**
加速列表的一等勾选单位，由微服务接口下发、本地缓存兜底，如"公共 CDN"、"Google 翻译"、"Github"。分组下含若干加速项目（叶子条目）。

**白名单**
改造版仅保留的三个加速分组：公共 CDN、Google 翻译、Github。白名单之外的分组一律不进界面，即使远端仍在下发。名称匹配忽略空格与大小写。

**Hosts 模式**
通过改写系统 hosts 文件把目标域名指向本地反向代理的加速方式，是改造版唯一的加速模式。系统代理与 PAC 模式不属于改造版。

**反向代理进程**
加速的实际执行者：本地 Yarp 反向代理子进程，配合本地根证书做转发。菜单栏的"启动/停止加速"控制的就是它。

**菜单栏图标（托盘图标）**
macOS 菜单栏常驻图标，加速启停与退出应用的唯一常驻入口。改造版在 macOS 上强制常驻，不受设置开关控制。

**后台隐藏态**
主窗口关闭（红色按钮）后进入的状态：Dock 图标消失、窗口隐藏、应用与加速继续运行。从菜单栏图标可恢复窗口或退出；Cmd+H 与此等价，Cmd+M 为最小化（保留 Dock 图标）。

**启动即加速**
应用打开即自动开始加速（等价于"程序启动时自动启动代理"默认开启），可在菜单栏中启停。
